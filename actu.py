#from pygooglenews import GoogleNews
#import datetime


#def get_actu(search):
 #   stories = []
  #  gn = GoogleNews(lang='fr')

   # search = gn.search(search)
    #newsites = search['entries']
    #for item in newsites:
     #   story = {
      #      'title':item.title,
       #     'link':item.link,
        #    'date':item.published,
         #   'source':item.source.title
       # }
        #stories.append(story)
    #return stories





#def top_news():
 #   stories = []
  #  gn = GoogleNews(lang='fr')

   # search = gn.top_news()
    #newsites = search['entries']
    #for item in newsites:
     #   story = {
      #      'title':item.title,
       #     'link':item.link,
        #    'date':item.published
        #}
        #stories.append(story)
    #return stories



#def get_cameroun():
 #   stories = []
  #  gn = GoogleNews(lang='fr', country='cameroon')

   # search = gn.topic_headlines('entertainment')
    #newsites = search['entries']
    #for item in newsites:
     #   story = {
      #      'title':item.title,
       #     'link':item.link,
        #    'date':item.published,
         #   'source':item.source.title

        #}
        #stories.append(story)
    #return stories


#def get_cote():
 #   stories = []
  #  gn = GoogleNews(lang='fr', country='cote+d+ivoire')

   # search = gn.topic_headlines('entertainment')
    #newsites = search['entries']
    #for item in newsites:
     #   story = {
      #      'title':item.title,
       #     'link':item.link,
        #    'date':item.published,
         #   'source':item.source.title

        #}
        #stories.append(story)
    #return stories



    